# Dependencies
node_modules/

# Environment variables (never commit real secrets)
.env
.env.local

# Local SQLite database files
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm

# Vercel
.vercel

# OS / editor noise
.DS_Store
*.log
